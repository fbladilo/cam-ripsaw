apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: cam-operator
  namespace: {{ cam_ns }}
spec:
{% if cam_version == 'v1.0' %}
  channel: release-v1
{% else %}
  channel: release-{{ cam_version }}
{% endif %}
  installPlanApproval: Automatic
  name: cam-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
